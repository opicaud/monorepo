apiVersion: opentelemetry.io/v1alpha1
kind: OpenTelemetryCollector
metadata:
  name: {{ include "shape-app-grpc.fullname" . }}-collector-sidecar
spec:
  mode: sidecar
  config: |
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: {{ .Values.openTelemetry.receiver.endpoint }}:4317
          http:
            endpoint: {{ .Values.openTelemetry.receiver.endpoint }}:4318
    processors:

    exporters:
      debug:
      {{- if .Values.openTelemetry.exporter.enabled }}
      otlp/exporter:
        endpoint: {{ .Values.openTelemetry.exporter.endpoint }}:4317
        tls:
          insecure: true
      {{- end }}
    service:
      pipelines:
        traces:
          receivers: [otlp]
          processors: []
          {{- if .Values.openTelemetry.exporter.enabled }}
          exporters: [debug, otlp/exporter]
          {{ else }}
          exporters: [debug]
          {{- end }}
