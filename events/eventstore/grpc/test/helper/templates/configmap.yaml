apiVersion: v1
kind: ConfigMap
metadata:
  name: grpc-eventstore-helper-api-config
data:
  config.yaml: |
    version: v2/beta
    event-store:
      protocol: grpc
      host: grpc-eventstore.default.svc.cluster.local
      port: 80