apiVersion: v1
kind: Service
metadata:
  labels:
    app: grpc-eventstore-helper-api
  name: grpc-eventstore-helper-api
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: grpc-eventstore-helper-api
  type: ClusterIP